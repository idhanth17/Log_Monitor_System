version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - db_data:/data
    environment:
      - DB_PATH=/data/logs.db
      - ADMIN_PASSWORD=admin123
    restart: unless-stopped

volumes:
  db_data:
